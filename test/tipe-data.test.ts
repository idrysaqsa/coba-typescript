describe('Data Type', () => {
    it('should must declare', () => {
        const name: string = 'Aqsa';
        const age: number = 20;
        const isStudent: boolean = true;

        console.info(name);
        console.info(age);
        console.info(isStudent);

    });
});