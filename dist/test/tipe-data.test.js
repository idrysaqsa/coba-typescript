"use strict";
describe('Data Type', () => {
    it('should must declare', () => {
        const name = 'Aqsa';
        const age = 20;
        const isStudent = true;
        console.info(name);
        console.info(age);
        console.info(isStudent);
    });
});
